package ru.serbis.lexer;

public enum  Tag {
            BITAND,  TYPE, BREAK, DO,
            ELSE, EQ, FALSE , GE,
            ID, IF, INDEX, LE ,
            NE, NUM, BITOR,
            REAL, TEMP,  TRUE,  WHILE,
            CLASS, RBRACE, LBRACE, RBRACKET,
            LBRACKET, COMMA, LOGAND, LOGOR,
            ASSIGNBASE, NOT, LESS, GROSS,
            FLOAT288, INT, FLOAT, SEMI, PLUS,

            IFFALSE, GOTO, TMPID;
}